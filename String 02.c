#include<stdio.h>
#include <string.h>int main()
{
    char ori[20],dup[20];
    char*2;
    printf("\nenter your name:");
    gets(ori);
    z=strcpy(dup,ori);
    printf("\nOriginal string is:%s",ori);
    printf("\nDuplicate string is:%s",dup);
    printf("\nThe value of z is:%s",z);
}